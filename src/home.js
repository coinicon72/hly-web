// @flow

import React from 'react';
import { Typography } from '@material-ui/core';

export default (props) => {
  return <Typography variant="display1" style={{ padding: 16 }}>欢迎！</Typography>
}
